# Top-level dependencies for scripts/ingest.py.
# Install into a local project venv — do not install into global/system
# Python (per ~/.claude/CLAUDE.md and the markitdown pipx isolation):
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
markitdown[pdf]
requests
pyyaml

# scripts/ask.py (single-paper Q&A) needs paper-qa, deliberately not listed
# here: it pulls a large dependency tree (litellm, tiktoken, etc.) that
# ingest.py doesn't need. Install separately if you're using Q&A:
#   pip install paper-qa

# webapp/ (Phase 4 browse-only local web UI) — thin FastAPI read layer over
# papers/, synthesis/, citations/ on disk. No database.
fastapi
uvicorn[standard]
jinja2
markdown
